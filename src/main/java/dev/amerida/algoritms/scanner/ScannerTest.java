package dev.amerida.algoritms.scanner;

import java.util.Scanner;

/**
 * Desarrolle un programa que permita cargar numeros enteros no repetidos a un arreglo de tamaño 10.
 * A continuacion debera determinar el maximo y minimo de los numeros del arreglo.
 * Tambien indicar cuantos valores no se encuentran presentes, e indicar la suma de los numeros del arreglo.
 */
public class ScannerTest {
    static void main() {
        Scanner scanner = new Scanner(System.in);

        scanner.close();
    }
}
